pipeline {
    agent {label "test"}
    triggers {
        cron('0 12 * * 1-5')
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
            }
        }
    }
}